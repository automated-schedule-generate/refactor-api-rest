import { TimetableGenerateRepository } from '@repositories';
import { Injectable } from '@nestjs/common';

@Injectable()
export class TimetableGenerateRepositoryImpl implements TimetableGenerateRepository {}
